//import { IApiArticles } from './../models/post.model';
/*import { DOMAIN, PORT, PROTOCOL } from './infrastructure.properties';
import { Observable } from 'rxjs';
import { HttpClient } from '@angular/common/http';
import { Injectable } from "@angular/core";



@Injectable({
    providedIn:'root'
})
    
export class PostService{

private API_GET_ALL_ARTICLES = `${PROTOCOL}://${DOMAIN}:${PORT}/articles`;

constructor(public http:HttpClient){}
*/

//método para llamar a todos los articles (o posts).

/*public getAllArticles() : Observable<any> {
    return this.http.get<any>('this.API_GET-ALL-ARTICLES');
}

    
}*/


